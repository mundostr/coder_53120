/**
 * Utilizamos el pequeño módulo de prueba que hemos subido
 * en nuestra cuenta de npmjs.com
 */

import { createHash } from 'coder53120_utils';

const hash = createHash('abc123');
console.log(hash);
